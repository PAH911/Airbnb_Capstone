import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const adminBookingSlice = createSlice({
  name: "adminBooking",
  initialState,
  reducers: {},
});

export const {} = adminBookingSlice.actions;

export default adminBookingSlice.reducer;
