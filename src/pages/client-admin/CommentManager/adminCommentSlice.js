import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const adminCommentSlice = createSlice({
  name: "adminComment",
  initialState,
  reducers: {},
});

export const {} = adminCommentSlice.actions;

export default adminCommentSlice.reducer;
