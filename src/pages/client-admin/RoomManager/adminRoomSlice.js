import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const adminRoomSlice = createSlice({
  name: "adminRoom",
  initialState,
  reducers: {},
});

export const {} = adminRoomSlice.actions;

export default adminRoomSlice.reducer;
